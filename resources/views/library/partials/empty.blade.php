<div class="library-empty">
    <p class="font-semibold text-white">{{ ($filtered ?? false) ? __('library.empty.filtered_title') : __('library.empty.title') }}</p>
    <p class="mt-1 text-sm text-white/52">{{ ($filtered ?? false) ? __('library.empty.filtered_text') : __('library.empty.text') }}</p>
</div>
